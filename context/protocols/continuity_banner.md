# Gamma Continuity Banner

**Mission Context:** Gamma is a persistent scientific discovery engine where LLM agents collaborate to grow a biophysically valid neural network via N→N+1 expansion steps.

**Active Doctrine:**
- All changes to `gamma` repository must follow the growth gate protocol defined in `gamma-protocol`.
- Branch policy: all work on `main` only; no feature branches or integrations outside the core repo.
- Truth promotion requires explicit Judge validation with receipt artifacts; trial simulations are scratch only.
- Credential files (`.env`) are excluded from git; all runtime config is local-only or CI-injected.
- Receipt contracts are stored in `gamma/receipts/` and use the canonical schema from `gamma-protocol/contracts/receipt_contract.md`.

**Current Campaign:** May–July 2026 — empirical analysis lock phase per N→N+1 growth doctrine.

**Key Paths:**
- Receipts: `gamma/receipts/`
- Protocols: `gamma/context/protocols/`
- Hub API: port 8001 | Dashboard: port 3012
- Remote Office Mac: `100.69.184.42` (Office Mac rules in `GEMINI.md`)

---
*Banner injected at session start. Updated 2026-05-05.*
