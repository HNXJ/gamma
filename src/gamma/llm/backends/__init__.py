"""Backend adapters package for Gamma LLM abstractions.

This subpackage contains concrete client implementations for various backends.
At present it only holds the `OpenAIMockLLM` used for unit‑test isolation.
"""
