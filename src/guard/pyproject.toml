[project]
name = "guard"
version = "0.1.0"
description = "A defensive agent harness for local markdown-driven CLI agents."
authors = [{ name = "Hamed Nejat" }]
dependencies = [
    "openai",
    "python-dotenv",
    "pydantic",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
