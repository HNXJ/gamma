family: gemma
target_specs:
  transformer_default:
    modules:
      - q_proj
      - k_proj
      - v_proj
      - o_proj
      - gate_proj
      - up_proj
      - down_proj
    merge_safety_constraints:
      - dtype_consistency
    default_rank: 8
    default_alpha: 16
    quantized_supported: true
